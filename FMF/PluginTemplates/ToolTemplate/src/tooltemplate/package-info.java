/**
 * These files comprise an example and template for FMF tools.
 * The example prints data from a data source table (to show how to access data), and 
 * writes the table dimensions to a new table and file (to show how to output).
 * The needed components are a menu, gui panel, and process. The example can be used as 
 * a starting point for a tool. 
 */
package tooltemplate;